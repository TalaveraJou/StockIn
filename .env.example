# Variables de entorno para el frontend (Vite)
# Copia como .env.local y rellena con los valores reales

# URL pública del servidor Express en Railway (sin barra final, sin /api)
# Ejemplo: https://stockin-production.up.railway.app
VITE_API_URL=http://localhost:3001
